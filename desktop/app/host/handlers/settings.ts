/**
 * 设置页（配置 / 代理 / 账号数据） 的后端 handler
 */
import type { HostContext } from "../context.ts";
import type { HostHandlerTable } from "../dispatch.ts";

export function createSettingsHandlers(_ctx: HostContext): HostHandlerTable {
  return {};
}
