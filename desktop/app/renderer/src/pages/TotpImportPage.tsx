/**
 * TOTP 密钥导入页（开发中占位）
 */
import type { ReactElement } from "react";
import { Typography } from "antd";

export function TotpImportPage(): ReactElement {
  return <Typography.Text type="secondary">TOTP 密钥导入（开发中）</Typography.Text>;
}
