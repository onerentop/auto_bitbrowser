/**
 * 设置页（配置 / 代理 / 账号数据）
 */
import type { ReactElement } from "react";
import { Typography } from "antd";

export function SettingsPage(): ReactElement {
  return <Typography.Text type="secondary">设置页（配置 / 代理 / 账号数据）（开发中）</Typography.Text>;
}
