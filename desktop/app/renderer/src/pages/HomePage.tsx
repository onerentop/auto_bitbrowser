/**
 * 首页（ixBrowser 窗口管理）
 */
import type { ReactElement } from "react";
import { Typography } from "antd";

export function HomePage(): ReactElement {
  return <Typography.Text type="secondary">首页（ixBrowser 窗口管理）（开发中）</Typography.Text>;
}
